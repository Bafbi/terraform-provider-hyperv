# Template for .terraformrc
# This file is used to generate .terraformrc with dynamic paths
# Run: mise run config
# Or: mise run setup
#
# The GOBIN_PATH placeholder will be replaced with the actual Go bin directory
# from your environment (either GOBIN or GOPATH/bin)

provider_installation {
  dev_overrides {
    "registry.terraform.io/taliesins/hyperv" = "GOBIN_PATH"
  }

  # For all other providers, install them directly from their origin provider
  # registries as normal.
  direct {}
}
